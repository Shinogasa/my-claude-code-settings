# Learning Journal

★ Predict（予測）と ★ Delta（差分）の記録。仕様は `rules/learning-mode.md` を参照。

<!-- ============================================================
     検証チェックポイント（このファイルに追記するたびに確認すること）

     判定: エントリ総数が20件に達しているか。（現在18件）

     達していれば: 段階2（SessionStart hook による間隔反復出題）の
     　　  導入を検討する。`[MISS]` 付きのエントリを優先出題の母集団とする。

     達していなければ: 追記を続ける。判定条件はまだ変更しない。

     前回判定の結果（2026-08-02 更新）:
     　　  「再設計以降のエントリ3件」の条件を達成した（学習ループ再設計 /
     　　  PR衝突 / dotfiles）。発火下限（1タスク1回）は機能していると判断し、
     　　  判定を上記の20件条件へ引き上げた。

     順序の自己点検: 件数が揃っていても、これは別途必ず確認すること。
     　　  各エントリの「予測」は、AI が結論・推奨・実装方針を提示する
     　　  前に取られたものか。後から取った予測が混じっていれば、件数が
     　　  揃っていてもこの機構は機能していない（pretesting効果が
     　　  抜け落ちた「抜け殻」のエントリになる）。件数チェックは
     　　  この失敗モードを検出できない。
     ============================================================ -->

> **⚠ このリポジトリは PUBLIC**
> 社名・組織名・プロジェクト名・リポジトリ名・内部ホスト名・内部URL・
> テーブル名・内部パス・業務コードを書かないこと。
> 技術的本質のみを一般化して記録する。判断に迷ったら書かない。
> 詳細は `rules/learning-mode.md` の「抽象化ルール」を参照。

## 記録フォーマット

```
## [YYYY-MM-DD] | タスク概要 [MISS]

**判断点:** 何を判断する場面だったか

**予測:** ユーザーの予測をそのまま記録

**実際:** 実際の判断・原因・リスク

**差分:** ★ Delta で返した内容
```

- 末尾に追記する（日付昇順を維持し、先頭に挿入しない）
- 予測を外した回は見出し末尾に `[MISS]` を付ける

---

## 記録

> 以下 2026-07-01 までの11件は、移行前の PRIVATE な作業環境リポジトリから
> 抽象化して取り込んだもの。業務固有情報を含む原本はそちらにアーカイブとして残している。
> 移行前の記録のため、項目名は当時の形式（問いかけ / 自分の説明 / 補足）のままとする。

## [2026-04-17] | 設定リポジトリ: 学習ルールの配置設計

**問いかけ:** 学習アウトプットのルールを既存ルールへの追記ではなく、独立した新規ファイルとして配置した理由は？

**自分の説明:**
今回の追加は作業中での問いかけによる「考える」という行為とは別軸のもので、作業完了後に学びを復習するものである。だから別物として独立したルール設定とした。

**補足:**
単一責任原則がルールファイル設計にも適用される好例。タイミング（途中 vs 完了後）と認知プロセス（思考 vs 言語化）の両方が異なるため、独立ファイルが正しい。

## [2026-04-21] | API パラメータの型設計 — nullable か デフォルト値付きか

**問いかけ:** 「なぜ `Boolean? = null` ではなく `Boolean = false` を選んだのか？」トレードオフを含めてどう説明する？

**自分の説明:**
バックエンドだと null の場合に既存の動作を保証するような実装になっているが、false にしたほうが明確に設定がわかる。トレードオフはいちいち書く必要があるところ。

**補足:**
「バックエンドのデフォルト値に暗黙依存しない」が核心。nullable だとバックエンド側がデフォルトを変更した場合に、呼び出し側の挙動が意図せず変わるリスクがある。default false により既存呼び出し元の書き換えは不要で、true を渡す箇所だけ明示する設計なのでコストも低い。

## [2026-04-22] | Kotlin `object` の `by lazy` 化（設定オブジェクトのトークン）

**問いかけ:** 今回 `val token: String = ...throw` ではなく `val token: String by lazy { ...throw }` を選んだ。「なぜ `by lazy` にしたの？」と聞かれたら、Kotlin `object` の初期化の仕組みを踏まえてどう説明する？

**自分の説明:**
オブジェクトは初回アクセス時に初期化が走る。今回利用しない変数があって環境変数に設定していなかった場合、現状だと exception が throw される。不要な環境変数をわざわざ設定する手間をなくしたいので、利用されるときに評価される lazy を利用した。

**補足:**
「初期化のタイミングを object ロード時から実使用時まで遅延させることで、関心の分離を実現した」とも言える。セットアップ用のスクリプトはトークンの関心事の外にいるのに、同じ設定オブジェクトに同居しているだけで巻き添えを食らっていた、という構造的な問題。JVM の `<clinit>` が全 val を一括評価する仕組みがその根本原因。

## [2026-04-23] | companion object vs トップレベル定数のスコープ選択

**問いかけ:** 今回 `MIN_SUPPORTED_VERSION` をトップレベル定数ではなく companion object に置いた。「なぜ companion object にしたの？トップレベルでも同じ結果じゃない？」と聞かれたら、スコープの観点からどう説明する？

**自分の説明:**
companion object はそのクラスに属する。トップレベル定数だと外部から読まれる可能性がある。今回の定数は外部で利用する想定もないので、このクラスのスコープだけで利用するように companion object にした。

**補足:**
トップレベル `private` はファイルスコープなので、同一ファイルに別クラスを追加した瞬間にアクセス可能になる。companion object + `private` ならクラススコープに完全に閉じるため、意図しない参照を構造的に防げる。「スコープを最小に保つ」原則の正確な適用。

## [2026-04-23] | モックテストの「意味の有無」を見分ける観点

**問いかけ:** モックベースで「件数だけ違う」テストが意味がある / 意味がないを分ける境界はどこにある？後輩から「テストケース分けたのにレビューで消されたのなんで？」と聞かれたら、「こういう観点で見分けられるよ」とどう説明する？

**自分の説明:**
返ってくる件数の違いを出すロジックが実際に動作している場合は意味がある、モックならば意味がないと考える。追加したテストが本当に意味のあるテストかどうかという観点で消すかどうかの判断。

**補足:**
核心は「**入力と出力の間で区別する処理が実際に一行でも走るか**」。当該ケースでは3点すべてが「動かないロジック」だった:
- モデル側に種別を示すフラグが無い（データを区別する手段が無い）
- 組み立て層に種別ごとの分岐が無い（挙動差分を生む処理が無い）
- モック境界が上位層のため、バージョンガード実装も観測できない

結果、context を分けても「モックで件数が違うリストを返す → そのまま通過する」だけで、プロダクションコードは何もしない。テストが観測しているのはテストデータ自身の差分のみ。「ロジックが走るか」という観点は、テストの命名やカバレッジ数字より信頼できる判断基準になる。

## [2026-05-14] | 負荷試験スクリプトのディレクトリ命名

**問いかけ:** 「エンドポイントパス体系」ではなく「四半期 × 機能名」で切る利点を、後から来たメンバーにどう説明する？

**自分の説明:**
負荷試験は機能リリース前に実施する。スクリプトを一括実行したいのでまとめている。

**補足:**
ディレクトリの粒度 = 実行の単位。パッケージ指定一発で関連スクリプトを全て回せるのが実用上の利点。エンドポイント体系はコードの所在としては論理的だが、運用（一括実行）の観点では不便。

## [2026-05-15] | `.git/` 配下を使う設計原則（個人設定 vs 共有設定）

**問いかけ:** 今回 `.gitignore` 編集ではなく `.git/info/exclude`、共有 hook ディレクトリではなく `.git/hooks/` を選んだ。「`.git/` 配下を使う設計判断には共通する原則がある」と気付いたか？

**自分の説明:**
個人設定は .git 配下使う。

**補足:**
本質を一言で捉えている。原則は「`.git/` 配下＝Git 自身の追跡対象外＝自分のクローンだけに効く」。
今後の判断軸として:
- 共有したい（チーム全員に効かせたい）→ tracked ファイル（`.gitignore`、`.githooks/` + `core.hooksPath` 等）
- 個人だけに効かせたい → `.git/` 配下（`.git/info/exclude`、`.git/hooks/`、ローカル `.git/config` 等）

この対比を持っておくと、「これは個人だけに効かせたい」と思った瞬間に `.git/` 配下の選択肢を即座に手繰れる。

## [2026-05-25] | 共有スクリプトを patch せずラッパー方式に切り替えた判断

**問いかけ:** 今回「共有スクリプトを自動 patch + pre-commit hook で誤コミットを防ぐ」案ではなく「個人用ラッパースクリプトを別途用意する」案を選んだ。「なぜラッパーにしたの？」と聞かれたら、トレードオフを含めてどう説明する？

**自分の説明:**
共通ファイルである実行スクリプトを更新したくなかったため。

**補足:**
本質を捉えている。さらに構造で見ると:
- patch 案 = 「誤コミットしうる状態」を作って **仕組みで防ぐ** 設計（pre-commit hook が安全装置）
- ラッパー案 = 「誤コミットしうる状態」自体を **作らない** 設計（共有ファイルが無改変）

「安全装置で守る」より「危険な状態を生まない」方が常に強い。後者は安全装置が壊れた・外された場合のフォールバックも不要だから、運用上のリスクが構造的にゼロになる。

トレードオフは実行コマンドの手癖が変わること。これは README + alias で誘導可能で、構造的安全と引き換えに払って妥当。この「仕組みで防ぐ vs そもそも起きない設計」の対比は、認可・バリデーション・型設計など他の場面でも応用が効く判断軸。

## [2026-06-01] | 分散DBの基礎: アクティブ-アクティブ / シャーディング / リードレプリカ

**問いかけ:** 「冗長性（障害への強さ）」の観点で、リードレプリカとシャーディングはどちらが有利か。それを "各台が持つデータの違い" からどう説明する？

**自分の説明:**
冗長性に関してはリードレプリカ。すべての DB で同じデータがあるので、どれかぶっ壊れても担保できる。

**補足:**
正確。核心は「**各台が持つデータの中身**」で全部説明がつくという点。
- リードレプリカ = 全台が**同じ完全なデータのコピー** → 1台死んでも他が肩代わり可（冗長性◎）。ただし書き込みは分散できない（どこに書いても全台反映が必要だから）
- シャーディング = 各台が**違うデータの一部**だけ持つ → 書き込みを並列化できる（書込スケール◎・容量突破）。が、1シャード死ぬとその範囲が止まる（冗長性△）

この一連で繋がった全体像:
- **アクティブ-アクティブ** = 全ノードが書ける構成。「昇格（フェイルオーバー）」という概念自体を消し、切替ダウンタイム・切替時データロストをゼロにする
- **シャーディング**（書込スケール）＋**リードレプリカ**（読取スケール＆冗長性）の**併用**が大規模 RDB の王道。「各シャードにレプリカをぶら下げる」ことで互いの弱点を補完する
- マネージド分散 SQL（Aurora DSQL 等）の本質は新技術の発明ではなく "統合と隠蔽"。シャーディング＋レプリカ＋アクティブ-アクティブを全部内包し、開発者には単一論理 DB に見せる。だから「実質無制限のスケール」「極めて高い可用性」「フェイルオーバー不要」が成立する

判断軸として持っておくべき対比:
- 読取が重い → リードレプリカ
- 書込が重い / 容量が限界 → シャーディング
- 冗長性が欲しい → レプリカ（同一データのコピーがあるから）

## [2026-06-29] | CDN 経由 API のキャッシュ動作確認と証跡設計

**問いかけ:** なぜ `x-cache: Hit` だけでなく `Age` ヘッダも証跡に含める設計にしたのか。`x-cache: Hit` だけでは何が不十分か。

**自分の説明:**
Age の値でキャッシュの鮮度がわかるから参考になる、という話。

**補足:**
方向性は正しい（Age = キャッシュされてからの経過秒数 = 鮮度指標）。
証跡としてもう一歩踏み込むと、Age が回を追って単調増加していることで
「同一のキャッシュエントリが時間経過とともに使い回されている（＝キャッシュが永続している）」
ことの裏付けになる。`x-cache: Hit` は「ヒットした」事実しか示さず、理屈上は毎回作り直しても
Hit と出うるため、Age の単調増加が "本当にキャッシュが効いている" ことの傍証として効く。

## [2026-07-01] | コンテナ内 SSH known_hosts の恒久登録先

**問いかけ:** 「なぜ `~/.ssh` ではなく `/etc/ssh/ssh_known_hosts` に置いたの？」を、"参照ファイルが2系統に割れていた" という原因と結びつけてどう説明する？

**自分の説明:**
そこに置くことにより、どの環境でも ssh が常にそのディレクトリも合わせて読んでくれる。そのため環境依存が出ない。

**補足:**
本質を捉えている。`~/.ssh/known_hosts`（ユーザー依存）や別途用意したランタイム用ディレクトリ配下の `known_hosts`（経路依存）は「誰が・どの経路で」に左右される環境依存の参照先。`/etc/ssh/ssh_known_hosts` はシステム全体の共通参照先で、ssh がどの経路でも必ず併せて読む。つまり「2系統の食い違い」を*解決*したのではなく、問題が発生しうる土俵の外に出た。当初の「コンテナ内でも使えるように明示的にした」という理解は方向A（経路を1つに統一する発想）で、実際に採った方向B（指定に依存しなくする）とは別物 — この違いに自分で違和感を持てたのが good。

## [2026-07-30] | バックエンドアプリ: キャッシュ回帰テストで保証できる範囲の見極め
`#kotlin` `#testing`

**問いかけ:** このテストがあれば、Redisの実際のTTLや有効期限切れの挙動もテストできてる、って理解でいい?

**自分の説明:**
このテストだとlabelUtilが呼ばれたかを保証していてキャッシュが使われたかを保証していない。TTLや有効期限切れに関しては別のテストケースじゃないとテストはできない。なぜなら有効期限切れまで待つ、みたいな動作を行なっていないため

**補足:**
的確。テストが観測しているのは「labelUtilの呼び出し回数」という副作用で、そこから「キャッシュがヒットした/しなかった」を推論しているにすぎない。TTL失効の検証には、時間経過(または時計のモック)を挟んだ別テストが必要で、このテストの責務範囲には含まれない。責務範囲を混同すると「このテストで守られているはず」という過信につながるので、テストが実際に何を検証しているかを常に言語化しておく価値がある。

---

## [2026-08-02] | 学習ループ再設計 — この設計が最初に壊れる箇所 [MISS]

**判断点:**
停止を `AskUserQuestion` 呼び出しに置き換えたが、「そのツールを呼ぶ判断」自体は AI の意志に残る。
この設計で最初に壊れるのはどこか。

**予測:**
AI 側の仕組み（ツールやシステムプロンプト）が変わったら壊れる。

**実際:**
最初に壊れるのは**発火点の選定**。ツールは呼ばれるが、開示の**後に**呼ばれるようになる。

**差分:**
ツール依存の指摘自体は正しい。ただしその故障は**うるさい** — ツールが消えれば呼び出しがエラーになり、
挙動が変われば即座に見える。気付ける故障は危険度が低い。

見落としていたのは**静かに壊れる方**。AI は問いを立てる前に既に答えを持っていることが多く、
「ここが判断点だ」と気付く頃には説明を書き始めている。その状態でツールを呼ぶと、
ツール呼び出し・停止・journal 追記のすべてが成立するのに、
ユーザーは既に推論を読んでいるため事前予測の効果だけが抜け落ちる。
しかも「3タスクでエントリ3件」という検証指標は通ってしまう。順序違反は回数に現れないため。

同じセッションで同型の失敗を3件観測した:
- 旧 Protégé — 記録先不在で書き込みが消え、1ヶ月誰も気付かなかった
- コンテナの read_only bind mount — 追記失敗がエージェント内部で握り潰される
- 今回の順序違反 — 検証指標だけ通って中身が空になる

一般化: **騒がしい故障は放置してよく、静かな故障だけが本当に危ない。**
そして静かな故障は、検証指標が「回数」や「存在」を見ている限り必ずすり抜ける。
検証は順序や因果を見る設計にしないと機能しない。

## [2026-08-02] | PR衝突の解消方針と、残存リスクの「静かさ」判定 [MISS]
`#git` `#セキュリティ` `#レビュー観点`

**問いかけ1（設計判断）:**
コンフリクトしたPRの解消を merge / rebase どちらで行うか。

**自分の説明:**
マージが良い。マージコミットが乗っても個人的には問題ないし、force push の方が事故の可能性がある。

**問いかけ2（レビュー力）:**
このPRをマージした後、最初に問題として顕在化するのはどこか。
4択 = ①学習ログがPUBLIC追跡になる ②旧symlinkが残り続ける ③起動ラッパーがPATH未通しで動かない ④学習モードの発火下限が機構的に強制されていない。

**自分の説明:**
1回目は②、2回目は③を選んだ。

**補足:**
前回のエントリで自分が立てた「静かな故障だけが本当に危ない」を、今回は自分の予測に適用できなかった。
②③はどちらも**騒がしい**側にある。

- ③は `command not found` で即死する。さらにこの起動ラッパーは、逆向きの設計（既定側から認証設定を抜く）
  だと「シェル統合が読まれなかったとき黙って別アカウントで動く」ため、**わざと騒がしい方**を選んだ設計だった。
  故障の可視性が設計目標そのものなので、これを最大リスクに置くと設計意図と矛盾する。
- ②は git が untracked 衝突で停止するか symlink を実ファイルで置換するかのどちらかで、移行元の実体は無傷。
  画面に何かが出て、復旧手段もある。
- ④は確かに静か。ただし後から機構を足せば回収できる。

見落としていたのは**もう1本の軸＝可逆性**。①だけが「起きた後に戻せない」。
PUBLIC リポジトリの git 履歴は、コミットを消しても fork・mirror・アーカイブ・既存クローンに残るため、
一度書けば取り消せない。しかも `.gitignore` から外れた瞬間、防御は rules に書かれた散文だけになり、
構造としては④と同じ「規約のみ・機構的強制なし」に落ちている。

一般化の更新: **「静か」だけでは足りない。静か × 不可逆が最悪の象限。**
静か×可逆は後から検知機構を足して回収できるが、静か×不可逆は初回で終わる。
リスクを並べるときは、可視性と可逆性の2軸で象限に置いてから優先順位を決める。

対策の方向: 抽象化ルールを散文から機構へ落とす（journal 差分を対象にした禁止パターン検査を
pre-commit / hooks 側に置く）。規約のままでは、疲れている日の1回で不可逆側に踏み込む。

### 何に注目すれば気づけたか

材料は最初の `git diff --stat` に出ていた。

```
.gitignore                    |   4 -
tasks/learning-journal.md     | 231 ++++++++
```

この2行が**セットで**出ているのが合図。「除外設定が減った」と「これまで追跡されていなかった
ファイルが大量に追加された」が同じ diff に同居している＝**追跡ポリシーの変更**が起きている。
片方だけなら誤検知もありうるが、両方揃えば断定してよい。

注目すべきは行数の非対称性。ポリシーの変更はたった4行で、600行超の計画書に埋もれる。
**diff の危険度は行数に比例しない。**

### 汎用化できる観察手順

1. **ポリシーファイルを最初に読む** — `.gitignore` / `.dockerignore` / 権限設定 / CORS /
   認証ミドルウェア / IAM ポリシー / `.env.example`。これらは「何を許すか」を定義する層で、
   小さい diff で境界を動かす。機能コードとは別パスでレビューする。
2. **各変更に undo コマンドを書けるか自問する** — 書けなければ one-way door。
   今回は3つ書けて（旧リンクは消して再実行、PATH は1行追加、発火下限は後から hook）、
   1つだけ書けなかった。**書けない項目が1つ見つかった時点で最優先が機械的に決まる**ので、
   判断力を使わずに答えが出る。疲れていても機能する形式なのが利点。
3. **防御の層の数を数える** — 変更前は3層（除外設定・別リポジトリへの隔離・抽象化規約）、
   変更後は1層（規約のみ）。多層防御が1層になった瞬間は、その1層がどれだけ丁寧に
   書かれていても危険度が跳ね上がる。しかも残った1層が「遵守に依存するもの」なら、
   それは防御ではなく**期待値**でしかない。
4. **書き込み主体を見る** — このログは AI が自動追記し、内容は業務セッション由来。
   「自動書き込み × 公開 × 業務由来」は継続的な漏洩チャネルであって、一度のレビューでは守れない。
   人間のレビューを経ずに公開先へ流れるパスがないかを探す。

### 肝になる知識: 公開 git 履歴が「消せない」実メカニズム

雰囲気ではなく具体的な経路がある。ここを知っていると①だけが別カテゴリだと即断できる。

| 経路 | なぜ消えないか |
|---|---|
| SHA 直参照 | ブランチを消してもオブジェクトは GC まで残り、SHA を知っていれば到達できる |
| PR の ref | ホスティング側が `refs/pull/N/head` を恒久保持する。**force push しても PR 由来の古いコミットは残る** |
| fork ネットワーク | fork 間でオブジェクトが共有保管され、元から消えても fork 経由で到達可能 |
| 既存クローン / ミラー | 手元・CI・社内ミラーに複製済み。中央からは制御できない |
| コード検索インデックス / scraper | push 直後に拾われ、削除は反映されない |

だから業界標準の扱いは「公開リポジトリに push した認証情報は、**履歴から除去するのではなく無効化する**」。
除去が信用できないことが前提になっている。

### メタ: 知識不足ではなく転移の失敗

「静かな故障だけが危ない」は前回のエントリで自分が書いた。**知識は既にあり、想起できなかった。**
実務ではこちらの失敗の方が圧倒的に多い。「知っている」と「その場で思い出せる」の間には溝があり、
負荷がかかるほど広がる。

対策は精神論ではなく外部化。上の手順2・3をチェックリストにしてレビュー時に必ず通す。
——今回の教訓そのものと同じ構造で、**規約（覚えておく）ではなく機構（必ず通る手順）にしないと守れない。**

## [2026-08-02] | dotfiles: cmux 外観設定の管理パス選定
`#dotfiles` `#設計判断` `#可観測性`

**問いかけ:** Application Support（cmux が最優先で読むが macOS 固有・上流 ghostty のアプリID依存）と XDG（OS非依存でリポジトリの XDG 準拠方針とも整合するが探索優先度が低い）、どちらを選ぶ？

**自分の説明:**
A。B だとそもそも聞いていなかったりエラーだったりがユーザーにわかりやすくないので気が付かない。

**補足:**
「壊れ方が見えるか」を選択基準に置いた判断。B（XDG）は Application Support 側にファイルが存在すると警告なく無視されるため、設定が効かないという症状だけが残り調査の起点が立たない。A は将来 cmux が自前アプリIDへ移行した際に一斉に効かなくなるので、異常として認識できる。「確実に動く」より「失敗が観測できる」を優先した設計判断で、可観測性を可用性より上に置く一般的な原則として応用が利く。なお A 固有のリスク（アプリID依存）は、README にトラブルシュート手順（`ghostty +show-config` で実効値を確認）を残すことで、壊れた際の調査コストを下げる形で緩和した。

## [2026-08-03] | dotfiles: 言語処理系のバージョン管理方針の選定 [MISS]
`#dotfiles` `#設計判断` `#ツールチェーン`

**判断点:** パッケージマネージャで導入した言語処理系を dotfiles に反映するにあたり、バージョン管理を外部のバージョンマネージャに委ねるか、単一グローバル版のまま処理系標準の機構に委ねるか。

**予測:** 外部バージョンマネージャに寄せる。既存の dotfiles は言語ごとに専用マネージャ（node / IaC ツール / JVM 系）を持っており、同じ流儀に揃えるのが一貫する。

**実際:** 外部マネージャは導入せず、パッケージマネージャ経由の単一版のみとした。当該処理系は近年のバージョンから**バージョン切り替えを処理系自身が内蔵**しており、プロジェクトの依存宣言ファイルを読んで必要な toolchain を自動取得・実行するため。

**差分:**

### 反例: そのピンは強制されない

外部マネージャで `1.26` をピン留めしたとする。そのプロジェクトの依存宣言ファイルに `1.28` が要求されていれば、マネージャが入れた 1.26 の処理系は宣言を読んで **1.28 を自動取得して実行する**。外部マネージャのピンは素通りされる。

一般化するとこうなる。**バージョン固定の機構を採用するときは「本当に強制されるか」を実測する。強制されない固定は、固定されているという誤った安心感を生むぶん、無いより悪い。** ピンが効かないマネージャに残るのは、宣言ファイルとマネージャ設定の二重管理コストだけになる。

### 予測が滑った経路: 「既存の流儀への一貫性」の誤適用

一貫性は良い論拠で、実際この判断でも最初に出てくる観点だった。滑ったのは論拠そのものではなく、**その流儀が成立していた前提条件を対象に当てはめ直さなかった**こと。

既存のマネージャ群が必要なのは、それぞれの処理系にバージョン切り替えが内蔵されていないから。今回の処理系はそれを内蔵している。前提が消えているので、同じ結論は導けない。

「他がそうしているから」は、**他がそうしている理由**まで遡らないと、理由が消滅した後も惰性で残る。既存パターンへの追従を論拠にするときは、そのパターンを生んだ制約が今も生きているかを1回確認する。これは新規ツール導入に限らず、コードベースの既存パターンを踏襲する判断すべてに効く。

### 予測に含まれていなかった観点: 真実の源の数

外部マネージャを挟むと、バージョンの真実の源が「依存宣言ファイル」と「マネージャ設定」の2つになる。両者が食い違ったとき、どちらが勝つかは処理系の実装依存で、しかも静かに勝敗が決まる（今回のケースでは宣言ファイル側が勝ち、マネージャ設定は無言で無視される）。設定機構を追加する判断では、機能の増減より先に**真実の源が増えないか**を見る。

## [2026-08-03] | 宣言ファイルと実環境の整合性チェックが落ちる原因の切り分け [MISS]
`#dotfiles` `#検証設計` `#サプライチェーン`

**判断点:** パッケージ宣言ファイルと実環境の整合性チェックコマンドが失敗する。どう解消するか。

**予測:** バージョン固定をしていないのだから、一括 upgrade すれば解消する。

**実際:** upgrade では解消しなかった。全パッケージを最新にして「更新可能なもの一覧」が空になった後も、チェックは落ちたままだった。

**差分:**

### そのチェックコマンドは2つの別問題を1つの終了コードに畳んでいる

| 問い | 意味 |
|---|---|
| 宣言されたものがインストール済みか | 環境の**再現性** |
| 最新か | メンテナンスの**鮮度** |

upgrade は後者しか動かせない。そして残った失敗は、実は**どちらでもない第3の状態**だった。サードパーティ配布元が「未信頼」のため、パッケージマネージャが**鮮度を判定できなかった**。インストールは済んでいる。古くもない。ただ「最新かどうか分からない」だけ。それが「未充足」として報告されていた。

### 一般化: 「判定不能」を「不合格」に畳む設計は、症状から対処法を逆算できなくする

出力は「インストールまたは更新が必要」。実際に必要だったのは**配布元の信頼宣言**で、インストールでも更新でも解決しない。メッセージが指す対処法が、真の対処法と無関係になっている。

だから対処の順序はこうなる。**エラーメッセージが提案する対処を試す前に、そのチェックが何を検査して終了コードを決めているかを確かめる。** メッセージは「検査に落ちた」ことしか正確に伝えておらず、「なぜ落ちたか」は往々にして推測で書かれている。今回、素直に upgrade を実行していたら、33パッケージを更新した上で同じ失敗を見ることになった（実際そうなった）。

### 検証コマンドを選ぶときの基準

再現性と鮮度は分けて実行する。混ぜると、コードを1行も変えていないのに時間経過だけで赤くなる検証ができあがる。赤が常態化すれば誰も見なくなり、**本当に環境が壊れているときの失敗を見逃す**。CI に載せる検証コマンドは「変更がなければ永遠に緑であり続けるか」を必ず確認する。

### 予測に含まれていなかった観点: 既定値の変化は「自分は何もしていない」ときに効いてくる

未信頼は、パッケージマネージャのメジャーバージョンアップで**非公式配布元が一律 untrusted に変わった**ことによる。設定を触っていなくても、ツール側の既定値変更で昨日まで通っていたものが落ちる。「自分は何も変えていないのに壊れた」ときは、依存ツールのメジャーバージョン更新履歴を最初に見る。

## [2026-08-03] | 学習用Webアプリ: 並行処理の題材選定と、実装差し替え時の故障点 [MISS]
`#設計判断` `#レビュー力` `#並行処理` `#レイヤードアーキテクチャ`

### 判断点1: 素朴なCRUDアプリで並行処理を学ぶ題材をどう仕込むか

**予測:** 集約API（1リクエストで複数の集計を並行取得する）。

**実際:** 同じ。ただし単独では不十分と判断し、常駐ワーカーを扱う段を追加して2段構えにした。

**差分（予測に含まれていなかった観点）: リクエストスコープの並行処理では、並行処理のライフサイクル管理を学べない**

`errgroup` 系の並行処理は、リクエスト終了時に context キャンセルで必ず回収される。つまり**リーク**という実務で最も事故る現象を、構造上体験できない。停止しない定期実行、書き手のいないチャネルでブロックし続けるワーカー、シャットダウン時に処理途中で殺されるジョブ——これらは常駐型でしか再現しない。

一般化するとこうなる。**「その機構で起きうる失敗を、その題材で再現できるか」を学習設計の選定基準に置く。** 安全に成功する題材は、失敗の形を教えない。題材選定では「何が学べるか」だけでなく「何が学べないか」を明示的に潰す。

### 判断点2: 永続化層の実装を差し替えたとき、設計がまずいと最も危険な壊れ方をするのはどこか

**予測:** テストコード全体。モックの型や前提が崩れて大量に落ちる。

**実際:** 上位層（HTTPハンドラ）のエラー分岐。

#### 反例: テストが全て緑のまま、本番だけが壊れる

テストが大量に落ちるのは「壊れた」のではなく「安全網が作動した」状態である。CIが赤くなればマージされない。実は最も安全な失敗モードだった。

反例はこうなる。ハンドラが低レベルドライバ固有の「該当行なし」エラーで分岐していたとする。ORM実装に差し替えると、実際に返るのは別の型のエラーになる。ところが上位層のユニットテストは永続化層をモックしており、そのモックは**ドメイン層のエラーを返すよう書かれている**ため、テストは全て通る。

結果、コンパイル成功・テスト全緑・しかし「存在しないリソースの取得」が404ではなく500を返す。

対照的に、interface にドライバ固有のトランザクション型が漏れていた場合はコンパイルすら通らない。うるさく壊れるものは怖くない。

一般化: **危険さは被害の大きさではなく、気づけなさで決まる。** 故障モードを列挙するときは「何が壊れるか」ではなく「壊れたときに気づけるか」で優先順位をつける。

#### 予測が滑った経路: 「影響範囲の広さ」を危険度と取り違えた

「テストコード全体」という予測は、影響を受ける行数で危険度を測っている。だが行数は検知可能性と無関係であり、むしろ**広く壊れるほど気づきやすい**。危険度の評価軸は影響範囲ではなく、検知までの時間である。

この取り違えは実務のリスク評価でも起きる。「広範囲に影響する変更」を慎重に扱うのは正しいが、それは検知しやすい方であって、本当に警戒すべきは**局所的で静かに振る舞いだけが変わる変更**の側にある。
