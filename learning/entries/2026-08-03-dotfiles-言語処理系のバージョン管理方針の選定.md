---
date: 2026-08-03
title: "dotfiles: 言語処理系のバージョン管理方針の選定"
result: miss
axis: null
---

# dotfiles: 言語処理系のバージョン管理方針の選定

`#dotfiles` `#設計判断` `#ツールチェーン`

**判断点:** パッケージマネージャで導入した言語処理系を dotfiles に反映するにあたり、バージョン管理を外部のバージョンマネージャに委ねるか、単一グローバル版のまま処理系標準の機構に委ねるか。

**予測:** 外部バージョンマネージャに寄せる。既存の dotfiles は言語ごとに専用マネージャ（node / IaC ツール / JVM 系）を持っており、同じ流儀に揃えるのが一貫する。

**実際:** 外部マネージャは導入せず、パッケージマネージャ経由の単一版のみとした。当該処理系は近年のバージョンから**バージョン切り替えを処理系自身が内蔵**しており、プロジェクトの依存宣言ファイルを読んで必要な toolchain を自動取得・実行するため。

**差分:**

### 反例: そのピンは強制されない

外部マネージャで `1.26` をピン留めしたとする。そのプロジェクトの依存宣言ファイルに `1.28` が要求されていれば、マネージャが入れた 1.26 の処理系は宣言を読んで **1.28 を自動取得して実行する**。外部マネージャのピンは素通りされる。

一般化するとこうなる。**バージョン固定の機構を採用するときは「本当に強制されるか」を実測する。強制されない固定は、固定されているという誤った安心感を生むぶん、無いより悪い。** ピンが効かないマネージャに残るのは、宣言ファイルとマネージャ設定の二重管理コストだけになる。

### 予測が滑った経路: 「既存の流儀への一貫性」の誤適用

一貫性は良い論拠で、実際この判断でも最初に出てくる観点だった。滑ったのは論拠そのものではなく、**その流儀が成立していた前提条件を対象に当てはめ直さなかった**こと。

既存のマネージャ群が必要なのは、それぞれの処理系にバージョン切り替えが内蔵されていないから。今回の処理系はそれを内蔵している。前提が消えているので、同じ結論は導けない。

「他がそうしているから」は、**他がそうしている理由**まで遡らないと、理由が消滅した後も惰性で残る。既存パターンへの追従を論拠にするときは、そのパターンを生んだ制約が今も生きているかを1回確認する。これは新規ツール導入に限らず、コードベースの既存パターンを踏襲する判断すべてに効く。

### 予測に含まれていなかった観点: 真実の源の数

外部マネージャを挟むと、バージョンの真実の源が「依存宣言ファイル」と「マネージャ設定」の2つになる。両者が食い違ったとき、どちらが勝つかは処理系の実装依存で、しかも静かに勝敗が決まる（今回のケースでは宣言ファイル側が勝ち、マネージャ設定は無言で無視される）。設定機構を追加する判断では、機能の増減より先に**真実の源が増えないか**を見る。
